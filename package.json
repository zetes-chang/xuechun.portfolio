{
  "name": "xuechun-react",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run prepare:data && vite",
    "build": "vite build",
    "build:fresh": "npm run prepare:data && vite build",
    "preview": "vite preview",
    "extract:state": "node scripts/extract-cargo-state.mjs",
    "normalize:pages": "node scripts/normalize-pages.mjs",
    "extract:assets": "node scripts/extract-assets.mjs",
    "download:assets": "node scripts/download-assets.mjs",
    "prepare:data": "npm run extract:state && npm run normalize:pages && npm run extract:assets"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "vite": "^5.4.11"
  }
}
